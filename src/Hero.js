import React from 'react'

const Hero = () => {
    return (
        <section className="hero">
            <nav>
                <h2>Welcome</h2>
                <button>Logout</button>
            </nav>
        </section>
    )
}

export default Hero
